#this the main python code page that will initialize and run the calculator

from app.calculator import calculator

if __name__ == "__main__": # the use of "__name__" such as __main__ indicates we are running
                           # the program directly
        

        calculator()